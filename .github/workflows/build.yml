##############
# chapter 01 #
##############
# name: my workflow                     # Workflow 이름
# on: [push]                            # Event 감지

# jobs:                                 # Job 설정
#   build:                              # Job ID
#     name: hello github action         # Job 이름
#     runs-on: ubuntu-18.04             # Job 가상환경 인스턴스
#     steps:                            # Steps
#       - name: checkout source code    # Step 이름
#         uses: actions/checkout@master # Uses를 통한 외부 설정 가져오기: 자신의 레포지토리 소스 받아오기

#       - name: echo Hello              # Step 이름
#         run: echo "Hello"             # Run을 통한 스크립트 실행: Hello 출력

##############
# chapter 02 #
##############
# name: React Build
# on: 
#   push:                               # master Branch에서 push 이벤트가 일어났을 때만 실행
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-18.04
#     steps:
#       - name: Checkout source code.   # 레포지토리 체크아웃
#         uses: actions/checkout@master

#       - name: Install Dependencies    # 의존 파일 설치
#         run: npm install

#       - name: Build                   # React Build
#         run: npm run build

##############
# chapter 03 #
##############
# name: React Cache Build 
# on: 
#   push:                               # master Branch에서 push 이벤트가 일어났을 때만 실행
#     branches:
#       - master

# jobs:
#   build:
#     runs-on: ubuntu-18.04
#     steps:
#       - name: Checkout source code.   # 레포지토리 체크아웃
#         uses: actions/checkout@master

#       - name: Cache node modules      # node modules 캐싱
#         uses: actions/cache@v1
#         with:
#           path: node_modules
#           key: ${{ runner.OS }}-build-${{ hashFiles('**/package-lock.json') }}
#           restore-keys: |
#             ${{ runner.OS }}-build-
#             ${{ runner.OS }}-

#       - name: Install Dependencies    # 의존 파일 설치
#         run: npm install

#       - name: Build                   # React Build
#         run: npm run build

##############
# chapter 04 #
##############
name: React S3 Build
on: 
  push:                               # master Branch에서 push 이벤트가 일어났을 때만 실행
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - name: Checkout source code.   # 레포지토리 체크아웃
        uses: actions/checkout@master

      - name: Cache node modules      # node modules 캐싱
        uses: actions/cache@v1
        with:
          path: node_modules
          key: ${{ runner.OS }}-build-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.OS }}-build-
            ${{ runner.OS }}-

      - name: Install Dependencies    # 의존 파일 설치
        run: npm install

      - name: Build                   # React Build
        run: npm run build

      - name: Deploy                  # S3에 배포하기
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        run: |
          aws s3 cp \
            --recursive \
            --region ap-northeast-2 \
            build s3://github-action-react-build-tutorial # 자신의 s3 버킷명으로 변경해주세요.
